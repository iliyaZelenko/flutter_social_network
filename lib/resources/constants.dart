import 'package:flutter/widgets.dart';

mixin Constants {
  static const standardScreenHorizontalPadding = EdgeInsets.symmetric(horizontal: 15);
  static const standardScreenPadding = EdgeInsets.only(left: 15, right: 15, top: 20);
}
