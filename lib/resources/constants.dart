import 'package:flutter/widgets.dart';

mixin Constants {
  // static const standardScreenHorizontalPadding = EdgeInsets.symmetric(horizontal: 15);
  static const standardScreenPadding = EdgeInsets.only(left: 20, right: 20);
}
